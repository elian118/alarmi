import 'package:flutter/material.dart';

class NewAlarmScreen extends StatelessWidget {
  const NewAlarmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('New Alarm')));
  }
}
