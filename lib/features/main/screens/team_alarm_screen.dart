import 'package:flutter/material.dart';

class TeamAlarmScreen extends StatelessWidget {
  const TeamAlarmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Team Alarm')));
  }
}
