import 'package:flutter/material.dart';

class CstPartLoading extends StatelessWidget {
  const CstPartLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
